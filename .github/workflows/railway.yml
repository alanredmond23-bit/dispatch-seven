name: Deploy to Railway
# DISABLED — primary deployment is ACA (aca-deploy.yml)
# Re-enable by removing the 'if: false' condition and adding RAILWAY_TOKEN secret
on:
  push:
    branches: [main]
jobs:
  deploy:
    if: false
    name: Railway Deploy
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - run: echo "Railway deployment disabled — using ACA"
