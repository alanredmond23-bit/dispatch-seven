name: Deploy to ACA
on:
  push:
    branches: [main]
    paths: ['backend/**', '.github/workflows/aca-deploy.yml']

jobs:
  deploy:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - name: Azure Login
        uses: azure/login@v2
        with:
          creds: ${{ secrets.AZURE_CREDENTIALS }}

      - name: Build and push to ACR
        run: |
          az acr build \
            --registry ${{ secrets.ACR_NAME }} \
            --image dispatch-seven-api:${{ github.sha }} \
            ./backend

      # Assign AcrPull to the ACA system-assigned managed identity (idempotent)
      # Replaces admin credential pattern — no --admin-enabled required
      - name: Grant ACA managed identity AcrPull on ACR
        run: |
          ACA_PRINCIPAL_ID=$(az containerapp show \
            --name dispatch-seven-api \
            --resource-group menagerie-rg \
            --query "identity.principalId" -o tsv)

          ACR_RESOURCE_ID=$(az acr show \
            --name ${{ secrets.ACR_NAME }} \
            --query "id" -o tsv)

          # Idempotent: only create if not already assigned
          EXISTING=$(az role assignment list \
            --assignee "$ACA_PRINCIPAL_ID" \
            --role "AcrPull" \
            --scope "$ACR_RESOURCE_ID" \
            --query "length(@)" -o tsv)

          if [ "$EXISTING" = "0" ]; then
            az role assignment create \
              --assignee "$ACA_PRINCIPAL_ID" \
              --role "AcrPull" \
              --scope "$ACR_RESOURCE_ID"
            echo "AcrPull role assigned to ACA managed identity"
          else
            echo "AcrPull role already assigned — skipping"
          fi

      - name: Deploy to ACA with env vars
        run: |
          az containerapp update \
            --name dispatch-seven-api \
            --resource-group menagerie-rg \
            --image ${{ secrets.ACR_NAME }}.azurecr.io/dispatch-seven-api:${{ github.sha }} \
            --registry-server ${{ secrets.ACR_NAME }}.azurecr.io \
            --registry-identity system \
            --set-env-vars \
              "PORT=3000" \
              "NODE_ENV=production" \
              "ANTHROPIC_API_KEY=${{ secrets.ANTHROPIC_API_KEY }}" \
              "BUDGET_CAP_USD=5.00" \
              "SUPABASE_URL=${{ secrets.SUPABASE_URL }}" \
              "SUPABASE_SERVICE_ROLE_KEY=${{ secrets.SUPABASE_SERVICE_ROLE_KEY }}" \
              "VOYAGE_API_KEY=${{ secrets.VOYAGE_API_KEY }}" \
              "LANGFUSE_PUBLIC_KEY=${{ secrets.LANGFUSE_PUBLIC_KEY }}" \
              "LANGFUSE_SECRET_KEY=${{ secrets.LANGFUSE_SECRET_KEY }}" \
              "MEM0_API_KEY=${{ secrets.MEM0_API_KEY }}" \
              "INNGEST_EVENT_KEY=${{ secrets.INNGEST_EVENT_KEY }}" \
              "INNGEST_SIGNING_KEY=${{ secrets.INNGEST_SIGNING_KEY }}" \
              "COURTLISTENER_API_KEY=${{ secrets.COURTLISTENER_API_KEY }}" \
              "SERP_API_KEY=${{ secrets.SERP_API_KEY }}" \
              "TAVILY_API_KEY=${{ secrets.TAVILY_API_KEY }}"
